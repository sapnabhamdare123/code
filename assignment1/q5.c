// construct a flowchart to show the step in finding the simple intrest on a given amount at a given rate of interest
# include <stdio.h>
int main()
{
int p,r,t,si;
printf("enter the number p,r,t");
scanf("%d %d %d",&p,&r,&t);
si=(p*r*t)/100;
printf("%d",si);
return 0;
}

